services:
  app:
    build:
      context: .
    container_name: go-container
    restart: unless-stopped
    ports:
      - "8081:8080"
    environment:
      - CGO_ENABLED=0
      - GOCACHE=/tmp/gocache
    volumes:
      - .:/app
    command: ["tail", "-f", "/dev/null"]
